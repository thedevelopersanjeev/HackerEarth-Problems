#include<bits/stdc++.h>

// Given a set of digits, find how many numbers of length len
// are possible whose value is less than num
int Solution::solve(vector<int> &digits, int len, int num){

    // Case 1 : len > num
    // Case 2 : len < num
    // Case 3 : len == num
    

}
